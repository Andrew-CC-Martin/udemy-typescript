const today = new Date();
today.getMonth();

const person = {
  age: 20
};

class Color {}
const red = new Color();

class Car {}
let car: Car = new Car();

let point: { x: number; y: number } = {
  x: 10,
  y: 5
};
